
{{define "title"}} Your Account {{end}}
{{define "main"}}
{{with .User}}
<table> 
    <tr>
        <th>Name</th> 
        <th>Email</th> 
        <th>created</th>
    </tr>
    
    <tr>
        <td>{{.Name}}</td>
        <td>{{.Email}}</td>
        <td>{{humanDate .Created}}</td>
    </tr>
</table>
{{end}} 
{{end}}